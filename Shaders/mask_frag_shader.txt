#version 330 core
#define PI 3.14159265359

in vec2 fragmentCoord;

layout (location = 0) out vec4 fragmentColor;

void main()
{

    float gray = 0.5;

    float y = pow(gray, (1/2.42));

    vec3 color = vec3(y);

	fragmentColor = vec4(color, 1.0);

}